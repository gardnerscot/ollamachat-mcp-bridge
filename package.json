{
  "name": "mcp-to-http-bridge",
  "version": "1.0.0",
  "description": "Bridges stdio-based MCP servers to HTTP so Android apps can call them",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --config mcp-config.json"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
